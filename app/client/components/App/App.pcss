body {
  color: #333;

  * {
    font-family: Verdana, sans-serif;
  }
}

html,
body {
  min-height: 100%;
}

:global {
  #root {
    display: flex;
    flex-direction: column;
    min-height: 100%;
  }
}

a,
a:visited {
  color: inherit;
  text-decoration: none;
}
